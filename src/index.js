// Importa la clase Dog en lugar de Car
import Dog from './dog.js';

// Crea una instancia de la clase Dog
const aDog = new Dog('Buddy', 'Golden Retriever', 2, 'Golden');

// Utiliza el método getDescription() para obtener la descripción del perro
console.log(aDog.getDescription());
